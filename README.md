# Jaydon Carter — Portfolio

Personal portfolio website built with React, Vite, and GitHub Pages.

Originally written in HTML/CSS. Recently completely refactored into modern React.

See `old-site` branch for legacy code.

## Development

```bash
npm install
npm run dev
```
